name: iac-scan

# Reusable Infrastructure-as-Code static analysis (Checkov) for BOTH tiers.
# Free everywhere. Scans Terraform, Kubernetes, CloudFormation, Helm, ARM, and
# more. Gate-only: CLI output, no SARIF upload.

on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: 'ubuntu-latest'
      directory:
        type: string
        default: '.'
      framework:
        description: 'Checkov framework filter, e.g. all, terraform, kubernetes, cloudformation.'
        type: string
        default: 'all'
      soft_fail:
        description: 'Report findings without failing the job.'
        type: boolean
        default: false
      quiet:
        type: boolean
        default: true
      skip_check:
        description: 'Comma-separated check IDs to skip, e.g. "CKV_AWS_1,CKV_AWS_2".'
        type: string
        default: ''
      enable_harden_runner:
        type: boolean
        default: true
      timeout_minutes:
        type: number
        default: 15

permissions: {}

jobs:
  iac-scan:
    name: iac-scan
    runs-on: ${{ inputs.runner }}
    timeout-minutes: ${{ inputs.timeout_minutes }}
    permissions:
      contents: read
    steps:
      - name: Harden runner
        if: ${{ inputs.enable_harden_runner }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false

      - name: Checkov
        uses: bridgecrewio/checkov-action@99bb2caf247dfd9f03cf984373bc6043d4e32ebf  # v12.1347.0
        with:
          directory: ${{ inputs.directory }}
          framework: ${{ inputs.framework }}
          soft_fail: ${{ inputs.soft_fail }}
          quiet: ${{ inputs.quiet }}
          skip_check: ${{ inputs.skip_check }}
          output_format: cli
